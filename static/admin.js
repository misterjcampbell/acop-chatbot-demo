Original file line numberDiff line numberDiff line change@@ -0,0 +1,180 @@// admin.js — Premium admin behaviors (FullCalendar + Mode B)(() => {  // config  const API_EVENTS = "/admin/events";  const API_TOGGLE_DATE = "/admin/toggle-date";  const API_BLOCK_SELECTED = "/admin/block-selected";  const API_UNBLOCK_SELECTED = "/admin/unblock-selected";  const API_TOGGLE_RANGE = "/admin/toggle-range";  const MAX_SELECT = 60; // safety: max contiguous days selectable  // state  let calendar;  let selectedDate = null;    // single click selection  let selectedRange = null;   // user drag select range  let rangeAction = 'block';  // what to do for range (block/unblock)  // UI elements  const prevBtn = document.getElementById('prevBtn');  const nextBtn = document.getElementById('nextBtn');  const calendarTitle = document.getElementById('calendarTitle');  const selectModeBtn = document.getElementById('selectModeBtn');  const blockBtn = document.getElementById('blockBtn');  const unblockBtn = document.getElementById('unblockBtn');  const blockRangeBtn = document.getElementById('blockRangeBtn');  const unblockRangeBtn = document.getElementById('unblockRangeBtn');  // utility: show toast simple  function toast(msg, type='info') {    // minimal toast: console + small alert in top-right    console[type === 'error' ? 'error' : 'log']('ADMIN:', msg);    // for now, brief visual cue: flash title    const orig = calendarTitle.textContent;    calendarTitle.textContent = `${orig} • ${msg}`;    setTimeout(()=>{ calendarTitle.textContent = orig; }, 1800);  }  function fixSticky(btn) {    if (!btn) return;    btn.addEventListener('mouseup', ()=> { btn.blur(); btn.classList.remove('active'); });    btn.addEventListener('mouseleave', ()=> { btn.classList.remove('active'); });    btn.addEventListener('touchend', ()=> { btn.blur(); btn.classList.remove('active'); });  }  // add sticky fixes  [selectModeBtn, blockBtn, unblockBtn, blockRangeBtn, unblockRangeBtn, prevBtn, nextBtn].forEach(fixSticky);  // Helpers for backend calls with axios (wrapped)  async function postJSON(url, payload) {    try {      const res = await axios.post(url, payload, { headers: { 'Content-Type': 'application/json' }, timeout: 10000 });      return res.data;    } catch (err) {      console.error('API error', err);      toast('Server error', 'error');      throw err;    }  }  // init FullCalendar  function initCalendar() {    const calendarEl = document.getElementById('calendar');    calendar = new FullCalendar.Calendar(calendarEl, {      initialView: 'dayGridMonth',      selectable: true,      height: 'auto',      editable: false,      headerToolbar: false,      selectMirror: true,      dayMaxEvents: 3,      select: function(info) {        // store selected range (FullCalendar: inclusive start, exclusive end sometimes)        const start = info.startStr.split('T')[0];        // endStr may equal next day's ISO; we will send both start and end and backend handles inclusivity        const end = info.endStr ? info.endStr.split('T')[0] : start;        selectedRange = { start, end };        // Visual feedback        toast(`Range selected ${start} → ${end}`);      },      dateClick: function(info) {        // Mode B: clicking selects (single)        selectedDate = info.dateStr;        // Visual highlight: create a temporary event or use selection styling        highlightSelection(selectedDate);        toast(`Selected ${selectedDate}`);      },      events: API_EVENTS,      eventDidMount: function(arg) {        // small accessible title        arg.el.setAttribute('title', arg.event.title || '');      },      datesSet: function(dateInfo) {        // update calendar title in top area        const { start } = dateInfo;        const monthName = start.toLocaleString(undefined, { month: 'long', year: 'numeric' });        calendarTitle.textContent = monthName;      },    });    calendar.render();    // prev/next wiring    prevBtn.addEventListener('click', () => calendar.prev());    nextBtn.addEventListener('click', () => calendar.next());  }  // highlight selected cell by adding temp event  function highlightSelection(dateIso) {    // remove existing temp highlight    calendar.getEvents().filter(e => e.id && e.id.startsWith('sel-')).forEach(e => e.remove());    // add invisible event at midday to create visible badge    calendar.addEvent({      id: 'sel-' + dateIso,      title: 'Selected',      start: dateIso + 'T12:00:00',      allDay: false,      display: 'background',      backgroundColor: 'rgba(0,128,255,0.12)'    });  }  // Callbacks for buttons  blockBtn.addEventListener('click', async () => {    if (!selectedDate) return toast('Select a date first');    try {      await postJSON(API_BLOCK_SELECTED, { date: selectedDate });      toast('Blocked ' + selectedDate);      selectedDate = null;      calendar.refetchEvents();    } catch {}  });  unblockBtn.addEventListener('click', async () => {    if (!selectedDate) return toast('Select a date first');    try {      await postJSON(API_UNBLOCK_SELECTED, { date: selectedDate });      toast('Unblocked ' + selectedDate);      selectedDate = null;      calendar.refetchEvents();    } catch {}  });  blockRangeBtn.addEventListener('click', async () => {    if (!selectedRange) return toast('Drag-select a range on the calendar first');    try {      await postJSON(API_TOGGLE_RANGE, { start: selectedRange.start, end: selectedRange.end, action: 'block' });      toast('Range blocked');      selectedRange = null;      calendar.refetchEvents();    } catch {}  });  unblockRangeBtn.addEventListener('click', async () => {    if (!selectedRange) return toast('Drag-select a range on the calendar first');    try {      await postJSON(API_TOGGLE_RANGE, { start: selectedRange.start, end: selectedRange.end, action: 'unblock' });      toast('Range unblocked');      selectedRange = null;      calendar.refetchEvents();    } catch {}  });  // select mode toggle (visual only)  selectModeBtn.addEventListener('click', () => {    const pressed = selectModeBtn.getAttribute('aria-pressed') === 'true';    selectModeBtn.setAttribute('aria-pressed', pressed ? 'false' : 'true');    selectModeBtn.classList.toggle('active');  });  // accessibility: keyboard support (arrow keys navigate months)  document.addEventListener('keydown', (e) => {    if (e.key === 'ArrowLeft') { calendar.prev(); }    if (e.key === 'ArrowRight') { calendar.next(); }  });  // init  document.addEventListener('DOMContentLoaded', () => {    initCalendar();  });})();